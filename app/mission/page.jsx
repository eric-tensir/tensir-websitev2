import { Shell } from "../components/site";
import { PagePlaceholder } from "../components/placeholder";

export const metadata = { title: "Mission — Tensir" };

export default function MissionPage() {
  return (
    <Shell>
      <PagePlaceholder title="Mission" note="[ mission statement — coming soon ]" />
    </Shell>
  );
}
