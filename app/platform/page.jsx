import { Shell } from "../components/site";
import { PagePlaceholder } from "../components/placeholder";

export const metadata = { title: "Platform — Tensir" };

export default function PlatformPage() {
  return (
    <Shell>
      <PagePlaceholder title="Platform" note="[ platform walkthrough — coming soon ]" />
    </Shell>
  );
}
